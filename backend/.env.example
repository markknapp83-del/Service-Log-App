# Healthcare Portal Backend Environment Configuration
# Development environment template - DO NOT use these values in production!

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
NODE_ENV=development
PORT=3001

# =============================================================================
# SECURITY CONFIGURATION - CHANGE ALL VALUES IN PRODUCTION!
# =============================================================================

# JWT Secrets (CRITICAL: Generate secure secrets for production)
# Use: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-token-secret-change-in-production

# Password hashing rounds (12+ recommended for production)
BCRYPT_ROUNDS=10

# Session configuration
SESSION_TIMEOUT=3600

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_PATH=./healthcare.db
AUDIT_DB_PATH=./audit.db

# Database performance settings
DB_CACHE_SIZE=64000
DB_BUSY_TIMEOUT=30000

# =============================================================================
# CORS AND SECURITY
# =============================================================================
# CORS origins (comma-separated list)
CORS_ORIGIN=http://localhost:3000,http://localhost:3005,http://localhost:5173

# Rate limiting
RATE_LIMIT_MAX=10000
RATE_LIMIT_WINDOW=900000
AUTH_RATE_LIMIT_MAX=50
AUTH_RATE_LIMIT_WINDOW=900000

# Request size limits
MAX_FILE_SIZE=10485760
MAX_REQUEST_SIZE=10mb

# =============================================================================
# LOGGING AND MONITORING
# =============================================================================
LOG_LEVEL=debug
ENABLE_METRICS=true
METRICS_PORT=9091

# =============================================================================
# HIPAA COMPLIANCE
# =============================================================================
ENABLE_AUDIT_LOGGING=true
AUDIT_LOG_RETENTION_DAYS=30
ENABLE_PHI_LOGGING=false
ENABLE_DATA_MASKING=true

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================
# Enable additional logging in development
DEBUG_MODE=true
VERBOSE_LOGGING=true

# Development database settings
DB_RESET_ON_START=false
DB_SEED_ON_START=false

# =============================================================================
# OPTIONAL INTEGRATIONS
# =============================================================================
# Email configuration (if needed)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password
# FROM_EMAIL=noreply@example.com

# External API keys (if needed)
# EXTERNAL_API_KEY=your-api-key
# EXTERNAL_API_URL=https://api.external-service.com

# =============================================================================
# PRODUCTION NOTES
# =============================================================================
# For production deployment:
# 1. Copy this file to .env.production
# 2. Change ALL security-related values (JWT secrets, etc.)
# 3. Set NODE_ENV=production
# 4. Configure proper CORS origins
# 5. Enable HTTPS and set security headers
# 6. Set appropriate rate limits
# 7. Configure monitoring and alerting
# 8. Enable audit logging with proper retention
# 9. Set up encrypted backups
# 10. Review and test all security settings